#ifndef TESTS_H
#define TESTS_H

// При запуске юнит тестов тестируются: сравнение контактов, вставка/удаление узлов,
// балансировка дерева, получение элементов по индексу, освобождение памяти.
// Проверяется сохранение свойств BST (бинарного дерева поиска) и корректность
// периодической балансировки.
void StartTest(void);

#endif // TESTS_H